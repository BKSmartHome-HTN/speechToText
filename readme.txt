install lib: https://www.npmjs.com/package/@react-native-voice/voice#manually-link-android
follow document to install module correctly
because this is a third-party module so we need to run in dev mode (if u are using expo)
eas build --profile development --platform android
npx expo start --dev-client
